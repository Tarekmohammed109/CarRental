<div class="container">
    <div class="col-sm"></div>
    <div class="col-sm-4">
        <h3 class="green">Success</h3>
        <p class="green">Thank you for your payment!</p>
        <p> Customer Email: {{charges.source.name}} </p>
        <p>Amount: ${{budget.total}}</p>
        <p>Card Ending: {{charges.source.last4}}</p>
        <p>Card: {{charges.source.brand}}</p>
        <a href="/">Go to Profile</a>
    </div>
    <div class="col-sm"></div>
</div>